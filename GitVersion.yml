mode: Mainline
branches:
  master:
    regex: ^(?:master|main)
  feature:
    regex: ^(?!master|main|pull)
    tag: beta
  pull-request:
    tag: rc
    tag-number-pattern: ''
ignore:
  sha: []
